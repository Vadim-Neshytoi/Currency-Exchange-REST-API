# Currency Exchange REST API

## Описание проекта

**Currency Exchange REST API** — учебный backend-проект, реализующий REST API для хранения валют, 
управления обменными курсами и конвертации денежных сумм.

Проект разработан без использования сторонних библиотек и веб-фреймворков и демонстрирует принципы построения многослойной архитектуры,
проектирования REST API, работы с HTTP, SQLite и объектной моделью Python.

---

## Основные возможности

Проект предоставляет REST API для:

- получения списка валют;
- получения информации о конкретной валюте;
- добавления новой валюты;
- получения списка обменных курсов;
- получения конкретного обменного курса;
- добавления нового обменного курса;
- обновления существующего обменного курса;
- конвертации денежных сумм между валютами.

При выполнении конвертации автоматически используется один из трёх сценариев:

- прямой обменный курс;
- обратный обменный курс;
- кросс-курс через USD.

---

## Используемые технологии

- Python 3.12+
- HTTPServer (`http.server`)
- SQLite3
- JSON
- Decimal
- Logging
- urllib.parse

Проект полностью построен на стандартной библиотеке Python и не использует сторонние зависимости.

---

## Архитектура проекта

Проект построен по многослойной архитектуре с разделением ответственности между слоями.

```
HTTP Request
      │
      ▼
 SimpleHandler
      │
      ▼
  Controller
      │
      ▼
DatabaseManager
      │
      ▼
    SQLite
      ▲
      │
 Domain Models
```

### Ответственность слоев

**Handler**

- обработка HTTP-запросов;
- разбор URL и параметров;
- валидация HTTP-запросов;
- формирование HTTP-ответов;
- сериализация объектов в JSON.

**Controller**

- реализация бизнес-логики;
- взаимодействие с DatabaseManager;
- выбор сценария конвертации;
- преобразование ошибок базы данных в бизнес-исключения.

**DatabaseManager**

- выполнение SQL-запросов;
- работа с SQLite;
- создание объектов моделей;
- отсутствие бизнес-логики.

**Domain Models**

Проект содержит модели предметной области:

- Currency
- ExchangeRate
- ExchangeResult

Каждая модель самостоятельно отвечает за:

- хранение собственного состояния;
- валидацию входных данных;
- инкапсуляцию через `property`;
- преобразование объекта в словарь (`to_dict()`).

**Exceptions**

Пользовательские исключения, обеспечивающие взаимодействие между слоями приложения.

---

## Требования

- Python 3.12 или выше

Проект не требует установки сторонних библиотек.

---

## Получение проекта

```bash
git clone https://github.com/<username>/Currency_Exchange.git

cd Currency_Exchange
```

---

## Запуск проекта

Запустить сервер:

```bash
python main.py
```

или

```bash
python3 main.py
```

После запуска REST API будет доступно по адресу:

**локально**

```
http://localhost:8000
```

**при развертывании на сервере**

```
http://<server-ip>:8000
```

---

## Проверка работы API

Данный репозиторий содержит только backend-часть приложения и не включает пользовательский интерфейс.

После запуска проекта работу REST API можно проверить с помощью:

- Postman;
- curl;
- Insomnia;
- любого другого HTTP-клиента.

Во время разработки для визуального тестирования использовался отдельный frontend, который не входит в данный репозиторий.
Ссылка на тестовый фронтенд - https://github.com/zhukovsd/currency-exchange-frontend

При наличии тестового frontend достаточно указать адрес работающего backend-сервера, 
после чего все запросы будут выполняться через разработанный REST API.

---

## REST API

| Метод | Endpoint | Назначение |
|--------|----------|------------|
| GET | `/currencies` | Получить список валют |
| GET | `/currency/{code}` | Получить валюту по коду |
| POST | `/currencies` | Добавить валюту |
| GET | `/exchangeRates` | Получить список обменных курсов |
| GET | `/exchangeRate/{pair}` | Получить обменный курс |
| POST | `/exchangeRates` | Добавить обменный курс |
| PATCH | `/exchangeRate/{pair}` | Обновить обменный курс |
| GET | `/exchange?from={code}&to={code}&amount={value}` | Выполнить конвертацию |

---

## Примеры запросов

### Получение списка валют

```
GET /currencies
```

Ответ:

```json
[
  {
    "id": 1,
    "code": "USD",
    "name": "United States Dollar",
    "sign": "$"
  }
]
```

---

### Добавление новой валюты

```
POST /currencies
```

Тело запроса (`application/x-www-form-urlencoded`):

```
name=Euro
code=EUR
sign=€
```

Ответ:

```json
{
  "id": 2,
  "code": "EUR",
  "name": "Euro",
  "sign": "€"
}
```

---

### Конвертация валют

```
GET /exchange?from=USD&to=EUR&amount=100
```

Ответ:

```json
{
  "baseCurrency": {
    "id": 1,
    "code": "USD",
    "name": "United States Dollar",
    "sign": "$"
  },
  "targetCurrency": {
    "id": 2,
    "code": "EUR",
    "name": "Euro",
    "sign": "€"
  },
  "rate": 0.910000,
  "amount": 100,
  "convertedAmount": 91.00
}
```